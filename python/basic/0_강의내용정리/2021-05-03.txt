T2 : 혼공파 07-3 pdf 378~

모듈만드는 법 : 파이썬 파일 만들고 외부에서 읽어들이면 됨
너무 간단하고 유연. 모듈을 코드로 인식하고 실행해버리는 문제 발생. 내장패키지 이름과 자신이 만든 모듈(패키지)이름이 같으면, 충돌발생. 내가 만든게 내부모듈보다 우선순위 높음ㅇㅇ. 패키지명하고 모듈하고 파일명 겹치게 만들면 안됨. 가령 os패키지만든다고 os 모듈 만들면 안된다는 뜻ㅋㅋㅋㅋ

패키지(거대한 모듈) - 모듈 - 함수 - 변수 

이번 장에서 만들거임ㅇㅇ
원의 반지름/넓이 구하기
모듈실행 안전장치 설치
패키지 만드는법 


프로그램의 시작과 끝은 메인임ㅇㅇ
파이썬은 메인은 다로 모듈로 존재함. 개신기하네. 
굳이 사용하지 않아도 상관은 없음

__name__ == "__main__" ?
__name__을 찍으면, '__main__'이 찍힘.
엔트리 포인트가 아니지만, 엔트리 포인트 파일 내에서 임프토 되었기에, 모듈 내 코드가 실행됨. __name__ 실행ㅎ면, 모듈의 이름을 나타냄.

protect. 자바의 protect의 부분?
__ㅇㅇ__ : 시스템에서 가져와서 사용하는 것들을 말함.



?? 뭐라는거지. 다시 들어볼까.
모듈의 이름 출력맞아이게맞아

모듈로 실행 : 모듈 자체로 실행
엔트리 포인트 실행 : 메인에 접근해서 실행
진입시점에 따라 결과를 다르게 할 수 있다는 것.



패키지 : 모듈이 모여서 만들어진것ㅇㅇ
모듈은 디렉토리 내의 파일끼리 왔다갔다한것
패키지는 다른 디렉토리내의 파일끼리도 왔다갔다 할 수 있음
즉, 디렉토리 간 왕래가 가능하단것.


__init__ : 패키지 내부 모듈 한꺼번에 가져오고 싶을 때 사용
3.3 버전까지는 반드시 존재해야하지만, 그 이후부터는 없어도 연결은 해준다고함. 하지만 호환성을 위해 넣는게 좋겠지 라는것. 내용적지않고 파일이 존재하는것만으로도 연결되는 경우가 있음ㅇㅇ. 자바의 생성자 개념. 즉 가장 먼저 실행. 생성자 영역부터 동작. 3.3 까지는 이게 있어야 반드시 패키지로 동작, 이후부터는 파이썬 파일만 있어도 패키지로 동작함


--------------------------
파이썬 코딩도장 - 모듈과패키지 - import로 모듈가져오기 가져오는게 나을듯 ㅇㅇ 이라고 하심ㅇㅇ

점프투파이썬 모듈 파트가 더 나음. 위키독스 확인
https://dojang.io/mod/page/view.php?id=2441
https://dojang.io/mod/page/view.php?id=2447
https://wikidocs.net/27

전자교재
https://drive.google.com/drive/u/0/folders/1t5j1BqZWT-8vGGM1K0rTZpRUUIWoxCOh
7장에 있음 CH05에 있음ㅇㅇ
이것들 확인 -----------------------------

T3 : 점프투파이썬 CH05? 6 pdf 42p

클래스의 self?
self : 자기자신 호출
생성자에 해당하는게 있을때 사용
밖에서 매개변수로 값 받고, 전역변수로 선언할 때 사용함
내부 함수에서 전체적으로 사용할 수 있게끔함.
전역변수로 보기에는 조금 힘들지만, 전역변수화하는 느낌과 비슷. 자바에서의 this 와 같음.

패키지 구조로 사용하는편이 공동작업이나 유지보수에 유리함. 

mvc패턴, mvt패턴
model view control(기준, 보여주는 디자인, 제어)
디자인은 디자인대로, 제어는 제어대로 등 서로 역할을 분할. 패키지처럼 다 따로 각자할것함.
model view(프론트엔드 개발자. 굉장히 많이 축소됨. t로 많이 통합흡수되었음.) t(템퍼레이츠? html, css, javascript를 의미)

set PYTHONPATH=C:/python1
# 환경변수에 경로를 일시적으로 등록하는것.

import game
game.sound.echo.echo_test() 
불가능. 불러와서 사용하는건 모듈단위로만 가능
패키지에 해당하는 디렉토리는 불러와서 사용하는게 불가능함ㅇㅇ

__init__ 용도?
해당 디렉터리가 패키지의 일부임을 알려줌
이게 없으면 패키지로 인식 X.

----------------------------
T5 : 혼공파 ch-7-3 텍스트데이터

텍스트데이터 : 메모장 문자열
바이너리데이터 : 2진데이터

01001000 : 72 : ascii letter H
ascii 인코딩방식 ㅇㅇ

텍스트데이터 100 : 3자리차지. 49 48 48 10진수로임
바이너리데이터 100 : 100. 01100100. 이걸 아스키코드로 변경하면 d가 되는데, 이처럼 텍스트 데이터로서의 의미를 알 수 없는 데이터를 바이너리 데이터라고함.

이미지, 동영상 등이 비정형. 텍스트 데이터로 표현 불가.
정형 : 정형화됨. 규격ㅇㅇ. 비정형 : 정형화되지않음

비정형데이터를 보기위해서는 별도의 프로그램 필요. 이미지의 경우, 그림판 포토샵 등.

인코딩 방식 : 아스키, UTF-8, EUC-KR 등
A를B로 변환 인코딩. 그 반대로 돌리는게 디코딩
urllib에서 b가 붙어있는 문자열이있음. wb와 같이. 
이게 바이너리데이터로 쓰겠다는뜻ㅇㅇ 2진수형식으로.

으악아찔해. 절려.

인터넷 이미지 저장
urllib, 이용


cmd> pip list
설치한 패키지와 버전확인

pip show 설치된모듈 
모듈이 설치된 위치

크롤링을 스크래핑이라고 함(해외)
우리나라는 스크랩을 신문지 한장을 스크랩이라고함
그리고 크롤링은 그 신문에서 내용 일부 추출하는것ㅇㅇ

---------------------
